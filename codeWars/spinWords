def spin_words(sentence):
    index = 0
    words = sentence.split(' ')
    for word in words:
        if len(word) >= 5:
            words[index] = word[::-1]
        index += 1
    words = ' '.join(words)
    print(words)
    return words
